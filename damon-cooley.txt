https://codepen.io/Ryohduck/pen/eYWZZpm?editors=1100
    1. What is the difference between an inline element and a block element?inline elements only take up 1 entire line while blocks take up lines inside of the block
    2. What happens when an element is positioned absolutely? it stays in the same place
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? padding?
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default, and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? 